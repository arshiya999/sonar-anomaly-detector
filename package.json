{
  "name": "aqua-vision",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:api": "cd backend && PYTHONPATH=. python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8765",
    "test:api": "cd backend && PYTHONPATH=. python3 -m unittest discover -s tests -v"
  }
}
